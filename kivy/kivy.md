## kivy文件的使用
> 在`kivy`中引入了`kv`文件：用于使ui代码和逻辑代码进行分离！

## kv文件规则
- kv文件命名规则:
    - 文件名：继承App类的类名(除去App三个字母且必须是小写) + .kv
- 代码分离规则：
    - 定义页面：使用`<>:`创建一个页面
    - 缩进：4个空格
## kv显示中文
  - kivy不支持中文，如果需要显示中文，需要自己指定字体
  - font_name = 'kvcn.ttc'

